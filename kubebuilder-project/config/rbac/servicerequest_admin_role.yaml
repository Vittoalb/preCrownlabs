# This rule is not used by the project kubebuilder-project itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over networking.example.com.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubebuilder-project
    app.kubernetes.io/managed-by: kustomize
  name: servicerequest-admin-role
rules:
- apiGroups:
  - networking.example.com
  resources:
  - servicerequests
  verbs:
  - '*'
- apiGroups:
  - networking.example.com
  resources:
  - servicerequests/status
  verbs:
  - get
