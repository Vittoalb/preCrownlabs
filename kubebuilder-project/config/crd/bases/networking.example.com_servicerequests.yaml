---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: servicerequests.networking.example.com
spec:
  group: networking.example.com
  names:
    kind: ServiceRequest
    listKind: ServiceRequestList
    plural: servicerequests
    singular: servicerequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceRequest è la definizione della risorsa custom
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceRequestSpec definisce i campi di input per la richiesta
            properties:
              allowSharedIP:
                type: boolean
              app:
                type: string
              component:
                type: string
              namespace:
                type: string
              targetPort:
                type: integer
            required:
            - app
            - component
            - namespace
            - targetPort
            type: object
          status:
            description: ServiceRequestStatus tiene traccia dello stato della richiesta
            properties:
              assignedPort:
                type: integer
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
